version: '3'

services:

  manager:
    image: jpralves/exjtecnologia-microservice:latest
    environment:
      - API_KEY=666
    ports:
      - 8081:8081
    volumes:
      - ./exjvolume:/env/exjtecnologia
      - ./exjvolume:/env/splunk
